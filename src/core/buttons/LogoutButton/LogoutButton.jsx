import React from 'react'
import { Button } from '@material-ui/core';

const LogoutButton = () => {
    return (
        <Button>
            Logout
        </Button>
    )
}

export default LogoutButton
